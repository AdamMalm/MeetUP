import React from "react";
import { ScrollView, Text } from "react-native";

const Chatpage = () => {
	return (
		<ScrollView>
			<Text>Chat Page</Text>
		</ScrollView>
	);
};

export default Chatpage;
